# Copy to .env.local for local overrides, or set in Vercel's dashboard
# (Project Settings -> Environment Variables) for the deployed build.

# Leave empty locally -- Vite's dev proxy handles /api automatically.
# Set to the deployed server's URL once it's live, e.g.:
# VITE_API_URL=https://tokimap-api.onrender.com
VITE_API_URL=